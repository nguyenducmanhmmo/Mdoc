#docs
  -if @docs.present?
    - @docs.each do |doc|
      %a{ href: (url_for [doc])}
        .doc
          %p.title= doc.title
          %p.date= time_ago_in_words(doc.created_at)
          %p.content= truncate(doc.content, length:50 )
  -else
    %h2 Create Doc
    %p Your first doc will be fun
    %button= link_to "New doc", new_doc_path
